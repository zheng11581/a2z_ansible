---
all:
  children:
    mysql:
      hosts:
        mysql57.db:
          ansible_connection: ssh
          ansible_ssh_user: root
    oracle:
      hosts:
        oracle18.db:
          ansible_connection: ssh
          ansible_ssh_user: root
    application:
      hosts:
        app.gw:
          ansible_connection: ssh
          ansible_ssh_user: root